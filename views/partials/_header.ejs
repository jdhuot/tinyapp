<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success" style="padding:1em 3em;">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup" style="justify-content: space-between;">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
      <% if (!user) { %>
      <div>
        <a href="/login" class="btn btn-dark btn-sm">Login</a>
        <a href="/register" class="btn btn-dark btn-sm">Register</a>
        </form>
      </div>
      <% } else { %>
      <div style="display:flex; flex-direction:row;">
        <h6 style="display:inline; padding:10px 20px 0 20px; color:#ffffff;">Signed in as: <%= user.email %></h6>
        <form method="post" action="/logout"><button type="submit" class="btn btn-outline-dark" style="display:inline;">Logout</button></form>
      </div>
      <% } %>
    </div>
  </nav>
</header>